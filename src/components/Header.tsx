import React from "react";
import styles from "./Header.module.css"

export default () => {
    return (
        <header className={styles.header}>
            <h1>React + TS Todo</h1>
        </header>
    )
}